#!/usr/bin/env bash

IMG=${DOCKER_ORG_NAME}/${PKG_NAME}-rpms
docker run -v $PWD/../build/SRPMS:/SRPMS $IMG cp -a /SRPMS /srpms
docker build -v $PWD/../build/SRPMS:/SRPMS -t $IMG --no-cache .
# docker run -v $PWD:/OUTPUT $IMG mv /work/build/rpm_build/SRPMS /OUTPUT
